// import React from 'react';
// import { shallow } from 'enzyme';

// import SettingsActivityList from '../index';

describe('<SettingsActivityList />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
