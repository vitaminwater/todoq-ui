// import { fromJS } from 'immutable';
// import { makeSelectActivityListDomain } from '../selectors';

// const selector = makeSelectActivityListDomain();

describe('makeSelectActivityListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
