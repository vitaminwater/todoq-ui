// import { fromJS } from 'immutable';
// import { makeSelectSettingsNewActivityDomain } from '../selectors';

// const selector = makeSelectSettingsNewActivityDomain();

describe('makeSelectSettingsNewActivityDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
