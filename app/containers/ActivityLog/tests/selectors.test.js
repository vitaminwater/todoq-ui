// import { fromJS } from 'immutable';
// import { makeSelectActivityLogDomain } from '../selectors';

// const selector = makeSelectActivityLogDomain();

describe('makeSelectActivityLogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
